machine:
  timezone:
    America/Los_Angeles
  ruby:
    version: 2.2.0
  environment:
    DATABASE_URL: mysql2://ubuntu@127.0.0.1:3306/ss_tweet_test

dependencies:
  pre:
    - rm -rf vendor/cache
    - gem install bundler

test:
  pre:
    - bundle exec LOTUS_ENV=test db migrate
  override:
    - bundle exec rspec